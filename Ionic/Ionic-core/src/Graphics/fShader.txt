#version 330

layout (location = 0) out vec4 color;
in vec4 pos;

in DATA
{
	vec4 pos;
	vec4 col;
} fs_in;

void main()
{
	float i = 1.0 / length(fs_in.pos.xy - (vec2(0, 0)));
	//color = (vec4(0.2, 0.3, 0.8, 1.0)) * i;
	color = (fs_in.col); 
}